# Optimized dependencies for Railway deployment
# CPU-only PyTorch to reduce memory footprint

# FastAPI stack
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.6

# CPU-only PyTorch (smaller than GPU version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchaudio==2.3.1+cpu

# Chatterbox TTS
# Note: Add proper package reference once available
# For now, this assumes manual installation or local package
